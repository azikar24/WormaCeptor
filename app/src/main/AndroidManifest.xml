<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright AziKar24 19/2/2023.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation,ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WormaCeptor.Splash"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Feature test activities -->
        <activity
            android:name=".LocationTestActivity"
            android:exported="false"
            android:theme="@style/Theme.WormaCeptor" />

        <activity
            android:name=".CookiesTestActivity"
            android:exported="false"
            android:theme="@style/Theme.WormaCeptor" />

        <activity
            android:name=".WebViewTestActivity"
            android:exported="false"
            android:theme="@style/Theme.WormaCeptor" />

        <activity
            android:name=".SecureStorageTestActivity"
            android:exported="false"
            android:theme="@style/Theme.WormaCeptor" />

        <activity
            android:name=".ComposeRenderTestActivity"
            android:exported="false"
            android:theme="@style/Theme.WormaCeptor" />
    </application>

</manifest>