package com.azikar24.wormaceptor.core.ui.components.divider

/**
 * Layout orientation for [WormaCeptorDivider].
 */
enum class DividerOrientation {
    /** Full-width horizontal rule — the default. */
    Horizontal,

    /** Vertical rule — pair with a `height()` modifier when placed in a [Row]. */
    Vertical,
}
