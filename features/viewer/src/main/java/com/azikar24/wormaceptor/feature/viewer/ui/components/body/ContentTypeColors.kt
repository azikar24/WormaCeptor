package com.azikar24.wormaceptor.feature.viewer.ui.components.body

/**
 * Color constants for content type indicators.
 * Colors are stored as hex strings for use in UI badges and labels.
 */
internal object ContentTypeColors {

    /** JSON content color (Amber). */
    const val JSON = "#F59E0B"

    /** XML content color (Purple). */
    const val XML = "#8B5CF6"

    /** HTML content color (Pink). */
    const val HTML = "#EC4899"

    /** Protobuf content color (Emerald). */
    const val PROTOBUF = "#10B981"

    /** Form data content color (Blue). */
    const val FORM_DATA = "#3B82F6"

    /** Multipart content color (Indigo). */
    const val MULTIPART = "#6366F1"

    /** Plain text content color (Gray). */
    const val PLAIN_TEXT = "#6B7280"

    /** Binary content color (Red). */
    const val BINARY = "#EF4444"

    /** PDF content color (Red-600). */
    const val PDF = "#DC2626"

    /** Image content color for all formats (Teal). */
    const val IMAGE = "#14B8A6"

    /** Unknown content color (Gray-400). */
    const val UNKNOWN = "#9CA3AF"
}
